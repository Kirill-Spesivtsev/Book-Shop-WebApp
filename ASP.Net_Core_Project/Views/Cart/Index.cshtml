@model IEnumerable<ASP.Net_Core_Project.Models.CartItem>

@{
    ViewData["Title"] = "Index";
}

<h1>Корзина</h1>

<!--
<p>
    <a asp-action="Create">Create New</a>
</p>
-->
<table class="table">
    <thead>
        <tr>
            <th></th>
            <th></th>
            <th>
                @Html.DisplayNameFor(model => model.Quantity)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <img src="~/Images/products/@item.Book.ImageName" alt="No image" width="100" />
                </td>
                <td>
                    @Html.DisplayFor(m => item.Book.Title)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Quantity)
                </td>
                <td>
                    <a asp-controller="Cart" asp-action="Delete"
                       asp-route-id="@item.Book.BookId" class="btn btn-outline-danger">
                        <i class="fas fa-trash-alt"></i>
                    </a>
                        @*@Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ })*@
                </td>
            </tr>
        }
    </tbody>
</table>
